name: weather_app
description: "A custom weather app/website that pulls from the National Weather Service"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  iso_duration: ^0.1.1
  provider: ^6.1.5
  intl: ^0.20.2
  geolocator: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
